PROTO VOID fact(n)
FUNC INT main() {
    INT test
    test := 4 * 2
    test := test / 4

    IF 0
    THEN
        test := test - 1
    ELSE
        test := 0
    FI
}

FUNC VOID fact(n) {
    IF n - 17
    THEN
        n := 32 - n
    ELSE
        n := n + 43
    FI

    RETURN n
}
